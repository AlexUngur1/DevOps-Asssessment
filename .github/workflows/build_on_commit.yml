name: GitHub Actions
run-name: ${{ github.actor }} is running Guthub Action
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: dotnet build
      - run: echo "🍏 This job's status is ${{ job.status }}."